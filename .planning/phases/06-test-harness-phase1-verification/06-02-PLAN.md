---
phase: 06-test-harness-phase1-verification
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/site-05-favicon.spec.ts
  - tests/site-06-noscript.spec.ts
  - tests/infra-01-build.spec.ts
  - tests/infra-02-deploy.spec.ts
  - .github/workflows/deploy.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SITE-05
  - SITE-06
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "SVG favicon link element exists on all pages and the favicon file resolves"
    - "Noscript fallback content renders when JavaScript is disabled"
    - "Build script (npx tsx build.ts) exits 0 and produces expected output files in dist/"
    - "deploy.yml has correct structure: push-to-main trigger, build/deploy jobs, actions/deploy-pages"
    - "CI test job gates deployment — tests must pass before deploy runs"
    - "REQUIREMENTS.md checkboxes accurately reflect verification status"
  artifacts:
    - path: "tests/site-05-favicon.spec.ts"
      provides: "Favicon verification tests for SITE-05"
      contains: "test.describe"
    - path: "tests/site-06-noscript.spec.ts"
      provides: "Noscript fallback tests for SITE-06"
      contains: "test.describe"
    - path: "tests/infra-01-build.spec.ts"
      provides: "Build script verification tests for INFRA-01"
      contains: "test.describe"
    - path: "tests/infra-02-deploy.spec.ts"
      provides: "Deploy workflow structure tests for INFRA-02"
      contains: "test.describe"
    - path: ".github/workflows/deploy.yml"
      provides: "CI pipeline with test job gating deployment"
      contains: "needs: test"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "tests/"
      via: "test job runs npm test before deploy"
      pattern: "npm test"
    - from: "tests/infra-02-deploy.spec.ts"
      to: ".github/workflows/deploy.yml"
      via: "reads and asserts workflow file structure"
      pattern: "deploy.yml"
---

<objective>
Write the remaining 4 requirement-mapped test files (SITE-05, SITE-06, INFRA-01, INFRA-02), add a test job to the CI pipeline that gates deployment, and fix stale REQUIREMENTS.md checkboxes.

Purpose: Complete the test suite covering all 7 Phase 1 requirements, ensure tests gate future deployments in CI, and bring REQUIREMENTS.md into alignment with actual verification status.
Output: 4 test files, updated deploy.yml with test job, corrected REQUIREMENTS.md checkboxes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-test-harness-phase1-verification/06-CONTEXT.md
@.planning/phases/06-test-harness-phase1-verification/06-01-SUMMARY.md

@package.json
@playwright.config.ts
@public/index.html
@public/super-word/index.html
@public/404.html
@build.ts
@.github/workflows/deploy.yml

<interfaces>
<!-- From Plan 01 — Playwright is installed, config exists, 3 test files pass -->
<!-- Executor has: playwright.config.ts with baseURL http://localhost:3000, webServer serving dist/ -->
<!-- Test pattern established: tests/*.spec.ts with test.describe('REQ-ID: description', ...) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SITE-05, SITE-06, INFRA-01, INFRA-02 test files</name>
  <files>tests/site-05-favicon.spec.ts, tests/site-06-noscript.spec.ts, tests/infra-01-build.spec.ts, tests/infra-02-deploy.spec.ts</files>
  <read_first>
    - public/index.html (favicon link element, noscript element)
    - public/super-word/index.html (favicon link, no noscript — check if present)
    - public/404.html (favicon link)
    - build.ts (entry points, output directory, what files it produces)
    - .github/workflows/deploy.yml (current structure to assert against)
    - tests/site-01-responsive.spec.ts (established test patterns from Plan 01)
  </read_first>
  <action>
Per D-02: One test file per requirement.

**tests/site-05-favicon.spec.ts** (SITE-05 — SVG favicon):
- For each page (`/`, `/super-word/`, `/404.html`):
  - Assert: `link[rel="icon"][type="image/svg+xml"]` element exists
  - Assert: the `href` attribute value is non-empty
  - Assert: fetching the favicon URL returns a 200 status (resolve relative href against page URL, use `page.request.get()`)
  - Assert: `link[rel="apple-touch-icon"]` element exists (apple-touch-icon is part of SITE-05 per requirements)

**tests/site-06-noscript.spec.ts** (SITE-06 — Noscript fallback):
- Create a new browser context with `javaScriptEnabled: false`
- Navigate to homepage (`/`)
- Assert: `noscript` element's content is rendered (visible text containing "JavaScript")
- Navigate to `/super-word/` and `/404.html` — assert pages load without errors (status 200) even with JS disabled
- Assert: the pages have meaningful content visible without JS (e.g., `<main>` contains text)

**tests/infra-01-build.spec.ts** (INFRA-01 — Build script):
- This is NOT a Playwright browser test — use Node.js `test` from `@playwright/test` or `execSync`
- Run `npx tsx build.ts` via `execSync` and assert exit code is 0
- After build, assert these files exist in `dist/`:
  - `dist/index.html`
  - `dist/super-word/index.html`
  - `dist/404.html`
  - `dist/styles/main.css`
  - `dist/shared/shell.js`
  - `dist/pages/home.js`
  - `dist/super-word/main.js`
  - `dist/pages/404.js`
  - `dist/favicon.svg`
- Use `import { existsSync } from 'node:fs'` and `import { execSync } from 'node:child_process'` for file system checks

**tests/infra-02-deploy.spec.ts** (INFRA-02 — GitHub Actions deployment):
- Per D-04: Read `.github/workflows/deploy.yml` as text and assert structural correctness
- Use `import { readFileSync } from 'node:fs'` to read the file
- Assert: file contains `on:` with `push:` and `branches: [main]`
- Assert: file contains a `build` job with `npx tsx build.ts` step
- Assert: file contains `actions/upload-pages-artifact`
- Assert: file contains a `deploy` job with `needs: build` (or `needs:` including `build`)
- Assert: file contains `actions/deploy-pages`
  </action>
  <acceptance_criteria>
    - tests/site-05-favicon.spec.ts exists and contains `test.describe('SITE-05`
    - tests/site-05-favicon.spec.ts checks `link[rel="icon"]` and `link[rel="apple-touch-icon"]`
    - tests/site-06-noscript.spec.ts exists and contains `test.describe('SITE-06`
    - tests/site-06-noscript.spec.ts creates context with `javaScriptEnabled: false`
    - tests/infra-01-build.spec.ts exists and contains `test.describe('INFRA-01`
    - tests/infra-01-build.spec.ts runs `npx tsx build.ts` or `tsx build.ts` via execSync
    - tests/infra-01-build.spec.ts checks `existsSync('dist/index.html')`
    - tests/infra-02-deploy.spec.ts exists and contains `test.describe('INFRA-02`
    - tests/infra-02-deploy.spec.ts reads `.github/workflows/deploy.yml`
    - tests/infra-02-deploy.spec.ts asserts `actions/deploy-pages` is in the file
    - `npm run build && npm test` exits 0 (all 7 test files pass)
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>4 test files covering SITE-05 (favicon), SITE-06 (noscript), INFRA-01 (build), INFRA-02 (deploy) all pass</done>
</task>

<task type="auto">
  <name>Task 2: Add test job to CI pipeline and fix REQUIREMENTS.md checkboxes</name>
  <files>.github/workflows/deploy.yml, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .github/workflows/deploy.yml (current job structure — build → deploy → lighthouse)
    - .planning/REQUIREMENTS.md (current checkbox states)
    - .planning/v1.0-MILESTONE-AUDIT.md (identifies 11 stale checkboxes and 7 partial requirements)
  </read_first>
  <action>
**1. Update `.github/workflows/deploy.yml`** per D-03:

Insert a `test` job between `build` and `deploy`. The test job:
- `needs: build` (runs after build)
- Uses `ubuntu-latest`
- Steps: checkout, setup-node (22, cache npm), npm ci, build (`npx tsx build.ts`), install Playwright (`npx playwright install --with-deps chromium`), run tests (`npx playwright test`)
- The `deploy` job's `needs` changes from `needs: build` to `needs: [build, test]`

Result structure: build → test → deploy → lighthouse

```yaml
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx tsx build.ts
        env:
          NODE_ENV: production
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test
```

Update deploy job: `needs: [build, test]`

**2. Fix REQUIREMENTS.md checkboxes:**

Per the milestone audit, 11 checkboxes are stale (marked `[ ]` but actually verified). Update to `[x]`:
- SITE-04: `[x]` (verified in Phase 3)
- SITE-07: `[x]` (verified in Phase 3)
- GAME-01 through GAME-05: all `[x]` (verified in Phase 2)
- LOOK-03: `[x]` (verified in Phase 4)
- LOOK-04: `[x]` (verified in Phase 3)
- LOOK-05: `[x]` (verified in Phase 4)
- LOOK-06: `[x]` (verified in Phase 3)

Also update the 7 Phase 1 requirements from `[ ]` with "pending" annotation to `[x]` since this phase adds automated verification:
- SITE-01: `[x]` — remove "pending automated verification" note
- SITE-02: `[x]` — remove "pending automated verification" note
- SITE-03: `[x]` — remove "pending automated verification" note
- SITE-05: `[x]` — remove "pending automated verification" note
- SITE-06: `[x]` — remove "pending automated verification" note
- INFRA-01: `[x]` — remove "pending automated verification" note
- INFRA-02: `[x]` — remove "pending automated verification" note

After updates: Only LOOK-02, LOOK-07, LOOK-08 remain `[ ]` (descoped from v1).
  </action>
  <acceptance_criteria>
    - .github/workflows/deploy.yml contains a `test:` job
    - deploy.yml test job contains `npx playwright test`
    - deploy.yml test job contains `npx playwright install --with-deps chromium`
    - deploy.yml deploy job `needs` includes `test` (e.g., `needs: [build, test]`)
    - REQUIREMENTS.md SITE-01 line starts with `- [x]`
    - REQUIREMENTS.md SITE-02 line starts with `- [x]`
    - REQUIREMENTS.md SITE-03 line starts with `- [x]`
    - REQUIREMENTS.md SITE-05 line starts with `- [x]`
    - REQUIREMENTS.md SITE-06 line starts with `- [x]`
    - REQUIREMENTS.md INFRA-01 line starts with `- [x]`
    - REQUIREMENTS.md INFRA-02 line starts with `- [x]`
    - REQUIREMENTS.md GAME-01 through GAME-05 all start with `- [x]`
    - REQUIREMENTS.md SITE-04 and SITE-07 start with `- [x]`
    - REQUIREMENTS.md LOOK-03, LOOK-04, LOOK-05, LOOK-06 start with `- [x]`
    - Only LOOK-02, LOOK-07, LOOK-08 remain as `[ ]` (descoped)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^\- \[x\]" .planning/REQUIREMENTS.md && grep "needs:.*test" .github/workflows/deploy.yml</automated>
  </verify>
  <done>CI pipeline has test job gating deployment; REQUIREMENTS.md checkboxes match actual verification status (20 of 23 checked, 3 descoped)</done>
</task>

</tasks>

<verification>
- `npm run build && npm test` exits 0 with all 7 test files passing
- `npx playwright test --list` shows tests from all 7 spec files
- deploy.yml has build → test → deploy → lighthouse job chain
- REQUIREMENTS.md has 20 `[x]` and 3 `[ ]` (descoped only)
</verification>

<success_criteria>
- SITE-05: favicon link and apple-touch-icon verified on all pages
- SITE-06: noscript fallback verified with JS disabled
- INFRA-01: build script runs successfully and produces all expected output files
- INFRA-02: deploy.yml structural correctness verified
- CI pipeline: test job gates deployment (deploy needs test)
- REQUIREMENTS.md: all checkboxes accurate — 20 verified, 3 descoped
</success_criteria>

<output>
After completion, create `.planning/phases/06-test-harness-phase1-verification/06-02-SUMMARY.md`
</output>
